{
  "version": 2,
  "builds": [
    {
      "src": "youtube/asgi.py",  // This should point to the ASGI entry point
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "youtube/asgi.py"  // Route all requests to ASGI app
    }
  ]
}
